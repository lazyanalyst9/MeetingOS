export function Ring({value,size=112,label}:{value:number,size?:number,label?:string}){const r=44,c=2*Math.PI*r;return <div className="relative shrink-0" style={{width:size,height:size}} role="img" aria-label={`${value}% complete`}><svg viewBox="0 0 100 100" className="-rotate-90"><circle cx="50" cy="50" r={r} fill="none" stroke="currentColor" className="text-white/10" strokeWidth="7"/><circle cx="50" cy="50" r={r} fill="none" stroke="currentColor" className="text-lime transition-all duration-500" strokeWidth="7" strokeLinecap="round" strokeDasharray={c} strokeDashoffset={c*(1-Math.min(value,100)/100)}/></svg><div className="absolute inset-0 grid place-content-center text-center"><b className="text-xl">{value}%</b>{label&&<span className="subtle text-[10px] uppercase">{label}</span>}</div></div>}
