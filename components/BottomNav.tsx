'use client';import { CalendarDays,ChartNoAxesColumnIncreasing,House,Settings,Sparkles } from 'lucide-react'
export type Tab='today'|'calendar'|'progress'|'coach'|'settings';const items:[Tab,string,typeof House][]=[['today','Today',House],['calendar','Calendar',CalendarDays],['progress','Progress',ChartNoAxesColumnIncreasing],['coach','Coach',Sparkles],['settings','Settings',Settings]]
export function BottomNav({tab,setTab}:{tab:Tab,setTab:(t:Tab)=>void}){return <nav aria-label="Main navigation" className="nav-safe fixed inset-x-0 bottom-0 z-40 border-t border-white/10 bg-[#0b1016]/95 px-2 pt-2 backdrop-blur-xl md:left-1/2 md:max-w-xl md:-translate-x-1/2 md:rounded-t-3xl"> <div className="flex justify-around">{items.map(([id,label,I])=><button key={id} onClick={()=>setTab(id)} aria-current={tab===id?'page':undefined} className={`tap flex w-16 flex-col items-center justify-center gap-1 text-[11px] ${tab===id?'text-lime':'text-muted'}`}><I size={20}/>{label}</button>)}</div></nav>}
