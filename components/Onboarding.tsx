'use client';import {useState} from 'react';import {addDays,localDate,pretty} from '@/lib/date'
export function Onboarding({finish}:{finish:(date:string)=>void}){const [date,setDate]=useState(localDate());return <main className="app-bg grid min-h-dvh place-items-center p-6"><div className="w-full max-w-md"><div className="mb-10 h-14 w-14 rounded-2xl bg-lime p-3 text-center text-xl font-black text-ink">82</div><p className="subtle mb-2 text-xs font-bold uppercase tracking-[.24em]">Welcome to</p><h1 className="text-4xl font-black leading-tight">82 Day<br/>Medium Challenge</h1><p className="subtle mt-4 text-lg">82 days. 11 commitments. One goal: consistency.</p><section className="card mt-9 p-5"><h2 className="text-xl font-bold">When do you want to start?</h2><div className="mt-4 grid grid-cols-2 gap-2"><button className="tap bg-lime font-bold text-ink" onClick={()=>setDate(localDate())}>Today</button><button className="tap bg-white/[.07] font-bold" onClick={()=>setDate(addDays(localDate(),1))}>Tomorrow</button></div><label className="subtle mt-4 block text-sm">Choose date<input className="field mt-2" type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><div className="mt-5 space-y-2 border-t border-white/10 pt-4 text-sm"><DateLine label="Start" value={date}/><DateLine label="Finish" value={addDays(date,81)}/><DateLine label="No-sweets finish" value={addDays(date,74)}/></div></section><button className="tap mt-5 w-full bg-lime text-base font-black text-ink" onClick={()=>finish(date)}>Start my challenge</button><p className="subtle mt-4 text-center text-xs">Your progress stays private on this device.</p></div></main>}
function DateLine({label,value}:{label:string;value:string}){return <div className="flex justify-between"><span className="subtle">{label}</span><b>{pretty(value)}</b></div>}
