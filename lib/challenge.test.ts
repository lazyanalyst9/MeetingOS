import {describe,expect,it} from 'vitest';import {addDays,dayNumber} from './date';import {completed,emptyLog,gymWeek,score,stats,sweetsStats} from './challenge';import {defaults} from './defaults'
const s=defaults().settings;describe('challenge calculations',()=>{it('uses inclusive local challenge days',()=>{expect(dayNumber('2026-08-10','2026-08-10')).toBe(1);expect(dayNumber('2026-08-10','2026-10-30')).toBe(82)});it('scores ten daily goals',()=>{const l=emptyLog('2026-08-10');expect(score(l,s)).toBe(0);l.wakeUpCompleted=true;expect(score(l,s)).toBe(10)});it('automates metric completion and calorie limit',()=>{const l={...emptyLog('x'),healthyEating:true,calories:2000,steps:10000,waterLiters:3,selfTimeMinutes:30};const c=completed(l,s);expect(c[1]).toBe(true);expect(c[3]).toBe(true);expect(c[6]).toBe(true);expect(c[8]).toBe(true);l.calories=2001;l.waterLiters=5.1;expect(completed(l,s)[1]).toBe(false);expect(completed(l,s)[8]).toBe(false)});it('calculates 80 percent streaks',()=>{const logs:Record<string,ReturnType<typeof emptyLog>>={};for(let i=0;i<3;i++){const l=emptyLog(addDays(s.challengeStartDate,i));Object.assign(l,{wakeUpCompleted:true,healthyEating:true,calories:1500,noSweets:true,selfTimeMinutes:30,sleptOnTime:true,productiveTasks:[{id:'1',text:'work',category:'Work'}],steps:10000,bhajans:true});logs[l.date]=l}expect(stats(logs,s,addDays(s.challengeStartDate,2)).current).toBe(3)});it('resets sweets but preserves best',()=>{const logs:Record<string,ReturnType<typeof emptyLog>>={};[true,true,false,true].forEach((v,i)=>logs[addDays(s.challengeStartDate,i)]={...emptyLog(addDays(s.challengeStartDate,i)),noSweets:v});expect(sweetsStats(logs,s.challengeStartDate,addDays(s.challengeStartDate,3))).toEqual({current:1,best:2})});it('counts gym days in Monday week',()=>{const logs:Record<string,ReturnType<typeof emptyLog>>={};['2026-08-10','2026-08-11','2026-08-13','2026-08-14','2026-08-16'].forEach(d=>logs[d]={...emptyLog(d),gym:true});expect(gymWeek(logs,'2026-08-12')).toBe(5)})})
